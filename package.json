{
  "name": "aura-clinic-piercing",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo da Aura Clinic Piercing: backend (Node/Express + Postgres) e frontend (React/Vite) como projetos independentes.",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,magenta \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "start": "npm --prefix backend run start",
    "build": "npm --prefix frontend run build",
    "lint": "biome lint .",
    "lint:fix": "biome lint --write .",
    "check": "biome check .",
    "format:check": "biome format .",
    "format": "biome format --write --staged .",
    "format:branch": "biome format --write --changed .",
    "format:all": "biome format --write .",
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "restore-admin": "npm --prefix backend run restore-admin --",
    "import:aura-jewelry": "npm --prefix backend run import:aura-jewelry --"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.6",
    "concurrently": "^9.2.1"
  }
}
