-- Marco de adoção do runner versionado para cada schema de clínica.
-- schema.sql continua responsável pelo estado legado durante a transição.
-- Não editar este arquivo depois de aplicado: o checksum é parte do contrato
-- de integridade do ledger.
SELECT 1;
